export interface ScheduleInterview {
    slotId: string; // Slot ID for the interview
    candidateId: string; // Candidate's ID
    status: string; // Interview status (e.g., "Scheduled", "Completed")
  }